# ESP-IDF build output and managed dependencies
build/
sdkconfig.old
managed_components/

# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
pubspec.lock

# Common compiled/generated artifacts
*.o
*.a
*.elf
*.bin
*.map
__pycache__/
*.pyc
.DS_Store

# Keep patched wifi static library — it's a build input, not output.
!esp32-c5/patched_libnet/libnet80211.a
